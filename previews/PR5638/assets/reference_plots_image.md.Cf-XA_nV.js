import{c as a,S as t,j as i,m as o}from"./chunks/framework.BMuthNHZ.js";const s="/previews/PR5638/assets/d9256a7.D2OHoCzG.png",n="/previews/PR5638/assets/b018409.Bccs4SHi.png",r="/previews/PR5638/assets/997dcbc.C7_7MScd.png",m=JSON.parse('{"title":"image","description":"","frontmatter":{},"headers":[],"relativePath":"reference/plots/image.md","filePath":"reference/plots/image.md","lastUpdated":null}'),l={name:"reference/plots/image.md"};function d(c,e,h,p,g,u){return t(),i("div",null,[...e[0]||(e[0]=[o(`<h1 id="image" tabindex="-1">image <a class="header-anchor" href="#image" aria-label="Permalink to &quot;image {#image}&quot;">​</a></h1><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># return a new figure, axis, and plot</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">f, ax, pl </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> image</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; kw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># creates an axis in a subfigure grid position</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">   ax, pl </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> image</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f[row, col], args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; kw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Creates a plot in the given axis or scene.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">       pl </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> image!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ax</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Union{Scene, AbstractAxis}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; kw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Creates a SpecApi plot, which can be used in \`S.Axis(plots=[plot])\`.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">     spec </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> SpecApi</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Image</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; kw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Plots an image on a rectangle bounded by <code>x</code> and <code>y</code>.</p><h2 id="Arguments" tabindex="-1">Arguments <a class="header-anchor" href="#Arguments" aria-label="Permalink to &quot;Arguments {#Arguments}&quot;">​</a></h2><p><strong>Conversion target:</strong> <code>x::Makie.EndPoints, y::Makie.EndPoints, image::AbstractMatrix{&lt;:Union{Float32, Float64, ColorTypes.Colorant}}</code></p><ul><li><p><code>image</code> An <code>AbstractMatrix{&lt;:Colorant}</code> defining the colors of an image, or an <code>AbstractMatrix{&lt;:Real}</code> defining colors through color mapping.</p></li><li><p><code>x, y, image</code> Defines the boundary of the image rectangle along with the image data. <code>x</code> and <code>y</code> can be a <code>Tuple{&lt;:Real, &lt;:Real}</code> or <code>ClosedInterval{&lt;:Real}</code>. When omitted they default to <code>0 .. size(image, 2)</code> along x and <code>0 .. size(image, 1)</code> along y (under the default <code>orientation = (:down, :right)</code>; non-default <code>orientation</code>s swap which array dim sizes which axis).</p></li></ul><a id="example-d9256a7"></a><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CairoMakie</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> FileIO</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">image</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">load</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">assetpath</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;cow.png&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)))</span></span></code></pre></div><img src="`+s+`" width="600px" height="450px"><p>Row 1 of the matrix appears at the visual top, column 1 at the left — matching what an image viewer would show. This works because <code>image</code> sets <code>yreversed = true</code> as a hint on the freshly-created axis.</p><div class="warning custom-block"><p class="custom-block-title"><code>image!</code> does not flip the y axis</p><p>The <code>yreversed = true</code> hint only applies when <code>image</code> creates a new axis. If you call <code>image!</code> to add into an existing axis, the y direction follows whatever that axis already had. Set <code>yreversed = true</code> on the axis yourself if you want the same upright orientation.</p></div><h3 id="Keeping-the-original-aspect-ratio" tabindex="-1">Keeping the original aspect ratio <a class="header-anchor" href="#Keeping-the-original-aspect-ratio" aria-label="Permalink to &quot;Keeping the original aspect ratio {#Keeping-the-original-aspect-ratio}&quot;">​</a></h3><p>By default the axis stretches the image to fill its allocated space. Pass <code>axis = (; aspect = DataAspect())</code> to keep the pixels at their natural 1:1 aspect ratio. See the &quot;Aspect ratios and automatic figure sizes&quot; tutorial for more on how aspect ratios interact with the rest of the layout. <a id="example-b018409"></a></p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CairoMakie</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> FileIO</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">image</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">load</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">assetpath</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;cow.png&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)); axis </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (; aspect </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> DataAspect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()))</span></span></code></pre></div><img src="`+n+'" width="600px" height="450px"><h3 id="Handling-different-image-layouts" tabindex="-1">Handling different image layouts <a class="header-anchor" href="#Handling-different-image-layouts" aria-label="Permalink to &quot;Handling different image layouts {#Handling-different-image-layouts}&quot;">​</a></h3><p><code>image</code> lays the matrix of pixels out onto a rectangle in data space. By convention we think of the quad as oriented top-to-bottom / left-to-right, which is why <code>image</code> sets <code>yreversed = true</code> as an axis hint when you call <code>image(array)</code>. The directions below are stated relative to that top-down y axis.</p><p>The default <code>orientation = (:down, :right)</code> matches the convention used by <code>FileIO.load</code>, NumPy, PIL, OpenCV and the like: <code>image[1, 1]</code> is the top-left pixel, the first array dim runs top-to-bottom, the second runs left-to-right. If your image was stored differently, for example because it was recorded by an unusual scientific instrument, you may need to adjust the <code>orientation</code> setting.</p><p><code>orientation</code> is a tuple <code>(d1, d2)</code> of directions describing how the first and second array dims run on the rectangle in the data space where y goes down and x to the right. Each entry is one of <code>:up</code>, <code>:down</code>, <code>:left</code>, <code>:right</code>; exactly one must be vertical and one horizontal. Below is a downscaled cow rendered at four different <code>orientation</code> values, with each cell labelled by its <code>(i, j)</code> index in the user matrix — following the indices in order shows which direction each array dim spreads. Note that <code>(:down, :right)</code> and <code>(:right, :down)</code> are not the same: in the first the rows of the matrix run downward and the columns rightward, while in the second the rows run rightward and the columns downward (a transpose of the layout).</p><p>Only <code>(:down, :right)</code> (the default) leads to a correct upright image of the cow; one of the other settings may be appropriate if the image you are dealing with was stored differently. <a id="example-997dcbc"></a></p><img src="'+r+'" width="1100px" height="950px"><h3 id="Explicit-x/y-bounds" tabindex="-1">Explicit x/y bounds <a class="header-anchor" href="#Explicit-x/y-bounds" aria-label="Permalink to &quot;Explicit x/y bounds {#Explicit-x/y-bounds}&quot;">​</a></h3><p>When you pass <code>image(xs, ys, mat)</code> with explicit <code>xs</code>/<code>ys</code>, those bounds replace the orientation-derived defaults, but <code>orientation</code> still determines the direction each array dim runs on the quad and <code>image</code> still sets <code>yreversed = true</code> as an axis hint. If the explicit y bounds are meant to be shown the conventional way (low y at the bottom), set <code>yreversed = false</code> yourself.</p><h2 id="Attributes" tabindex="-1">Attributes <a class="header-anchor" href="#Attributes" aria-label="Permalink to &quot;Attributes {#Attributes}&quot;">​</a></h2><h3 id="Generic-Attributes" tabindex="-1">Generic Attributes <a class="header-anchor" href="#Generic-Attributes" aria-label="Permalink to &quot;Generic Attributes {#Generic-Attributes}&quot;">​</a></h3><h4 id="clip_planes" tabindex="-1"><code>clip_planes</code> <a class="header-anchor" href="#clip_planes" aria-label="Permalink to &quot;`clip_planes` {#clip_planes}&quot;">​</a></h4><p><strong>Default:</strong> <code>@inherit clip_planes automatic</code></p><p>Clip planes offer a way to do clipping in 3D space. You can set a Vector of up to 8 <code>Plane3f</code> planes here, behind which plots will be clipped (i.e. become invisible). By default clip planes are inherited from the parent plot or scene. You can remove parent <code>clip_planes</code> by passing <code>Plane3f[]</code>.</p><h4 id="depth_shift" tabindex="-1"><code>depth_shift</code> <a class="header-anchor" href="#depth_shift" aria-label="Permalink to &quot;`depth_shift` {#depth_shift}&quot;">​</a></h4><p><strong>Default:</strong> <code>0.0</code></p><p>Adjusts the depth value of a plot after all other transformations, i.e. in clip space, where <code>-1 &lt;= depth &lt;= 1</code>. This only applies to GLMakie and WGLMakie and can be used to adjust render order (like a tunable overdraw).</p><h4 id="fxaa" tabindex="-1"><code>fxaa</code> <a class="header-anchor" href="#fxaa" aria-label="Permalink to &quot;`fxaa` {#fxaa}&quot;">​</a></h4><p><strong>Default:</strong> <code>false</code></p><p>Adjusts whether the plot is rendered with fxaa (fast approximate anti-aliasing, GLMakie only). Note that some plots implement a better native anti-aliasing solution (scatter, text, lines). For them <code>fxaa = true</code> generally lowers quality. Plots that show smoothly interpolated data (e.g. image, surface) may also degrade in quality as <code>fxaa = true</code> can cause blurring.</p><h4 id="model" tabindex="-1"><code>model</code> <a class="header-anchor" href="#model" aria-label="Permalink to &quot;`model` {#model}&quot;">​</a></h4><p><strong>Default:</strong> <code>automatic</code></p><p>Sets a model matrix for the plot. This overrides adjustments made with <code>translate!</code>, <code>rotate!</code> and <code>scale!</code>.</p><h4 id="overdraw" tabindex="-1"><code>overdraw</code> <a class="header-anchor" href="#overdraw" aria-label="Permalink to &quot;`overdraw` {#overdraw}&quot;">​</a></h4><p><strong>Default:</strong> <code>false</code></p><p>Controls if the plot will draw over other plots. This specifically means ignoring depth checks in GL backends</p><h4 id="space" tabindex="-1"><code>space</code> <a class="header-anchor" href="#space" aria-label="Permalink to &quot;`space` {#space}&quot;">​</a></h4><p><strong>Default:</strong> <code>:data</code></p><p>Sets the transformation space for box encompassing the plot. See <code>Makie.spaces()</code> for possible inputs.</p><h4 id="ssao" tabindex="-1"><code>ssao</code> <a class="header-anchor" href="#ssao" aria-label="Permalink to &quot;`ssao` {#ssao}&quot;">​</a></h4><p><strong>Default:</strong> <code>false</code></p><p>Adjusts whether the plot is rendered with ssao (screen space ambient occlusion). Note that this only makes sense in 3D plots and is only applicable with <code>fxaa = true</code>.</p><h4 id="transformation" tabindex="-1"><code>transformation</code> <a class="header-anchor" href="#transformation" aria-label="Permalink to &quot;`transformation` {#transformation}&quot;">​</a></h4><p><strong>Default:</strong> <code>:automatic</code></p><p>Controls the inheritance or directly sets the transformations of a plot. Transformations include the transform function and model matrix as generated by <code>translate!(...)</code>, <code>scale!(...)</code> and <code>rotate!(...)</code>. They can be set directly by passing a <code>Transformation()</code> object or inherited from the parent plot or scene. Inheritance options include:</p><ul><li><p><code>:automatic</code>: Inherit transformations if the parent and child <code>space</code> is compatible</p></li><li><p><code>:inherit</code>: Inherit transformations</p></li><li><p><code>:inherit_model</code>: Inherit only model transformations</p></li><li><p><code>:inherit_transform_func</code>: Inherit only the transform function</p></li><li><p><code>:nothing</code>: Inherit neither, fully disconnecting the child&#39;s transformations from the parent</p></li></ul><p>Another option is to pass arguments to the <code>transform!()</code> function which then get applied to the plot. For example <code>transformation = (:xz, 1.0)</code> which rotates the <code>xy</code> plane to the <code>xz</code> plane and translates by <code>1.0</code>. For this inheritance defaults to <code>:automatic</code> but can also be set through e.g. <code>(:nothing, (:xz, 1.0))</code>.</p><h4 id="transparency" tabindex="-1"><code>transparency</code> <a class="header-anchor" href="#transparency" aria-label="Permalink to &quot;`transparency` {#transparency}&quot;">​</a></h4><p><strong>Default:</strong> <code>false</code></p><p>Adjusts how the plot deals with transparency. In GLMakie <code>transparency = true</code> results in using Order Independent Transparency.</p><h4 id="visible" tabindex="-1"><code>visible</code> <a class="header-anchor" href="#visible" aria-label="Permalink to &quot;`visible` {#visible}&quot;">​</a></h4><p><strong>Default:</strong> <code>true</code></p><p>Controls whether the plot gets rendered or not.</p><h3 id="DataInspector-Attributes" tabindex="-1">DataInspector Attributes <a class="header-anchor" href="#DataInspector-Attributes" aria-label="Permalink to &quot;DataInspector Attributes {#DataInspector-Attributes}&quot;">​</a></h3><h4 id="inspectable" tabindex="-1"><code>inspectable</code> <a class="header-anchor" href="#inspectable" aria-label="Permalink to &quot;`inspectable` {#inspectable}&quot;">​</a></h4><p><strong>Default:</strong> <code>@inherit inspectable</code></p><p>Sets whether this plot should be seen by <code>DataInspector</code>. The default depends on the theme of the parent scene.</p><h4 id="inspector_label" tabindex="-1"><code>inspector_label</code> <a class="header-anchor" href="#inspector_label" aria-label="Permalink to &quot;`inspector_label` {#inspector_label}&quot;">​</a></h4><p><strong>Default:</strong> <code>automatic</code></p><p>Sets a callback function <code>(plot, index, position) -&gt; string</code> which replaces the default label generated by DataInspector.</p><h3 id="Colormapping-Attributes" tabindex="-1">Colormapping Attributes <a class="header-anchor" href="#Colormapping-Attributes" aria-label="Permalink to &quot;Colormapping Attributes {#Colormapping-Attributes}&quot;">​</a></h3><h4 id="alpha" tabindex="-1"><code>alpha</code> <a class="header-anchor" href="#alpha" aria-label="Permalink to &quot;`alpha` {#alpha}&quot;">​</a></h4><p><strong>Default:</strong> <code>1.0</code></p><p>The alpha value of the colormap or color attribute. Multiple alphas like in <code>plot(alpha=0.2, color=(:red, 0.5))</code>, will get multiplied.</p><h4 id="colormap" tabindex="-1"><code>colormap</code> <a class="header-anchor" href="#colormap" aria-label="Permalink to &quot;`colormap` {#colormap}&quot;">​</a></h4><p><strong>Default:</strong> <code>[:black, :white]</code></p><p>Sets the colormap that is sampled for numeric <code>color</code>s. <code>PlotUtils.cgrad(...)</code>, <code>Makie.Reverse(any_colormap)</code> can be used as well, or any symbol from ColorBrewer or PlotUtils. To see all available color gradients, you can call <code>Makie.available_gradients()</code>.</p><h4 id="colorrange" tabindex="-1"><code>colorrange</code> <a class="header-anchor" href="#colorrange" aria-label="Permalink to &quot;`colorrange` {#colorrange}&quot;">​</a></h4><p><strong>Default:</strong> <code>automatic</code></p><p>The values representing the start and end points of <code>colormap</code>.</p><h4 id="colorscale" tabindex="-1"><code>colorscale</code> <a class="header-anchor" href="#colorscale" aria-label="Permalink to &quot;`colorscale` {#colorscale}&quot;">​</a></h4><p><strong>Default:</strong> <code>identity</code></p><p>The color transform function. Can be any function, but only works well together with <code>Colorbar</code> for <code>identity</code>, <code>log</code>, <code>log2</code>, <code>log10</code>, <code>sqrt</code>, <code>logit</code>, <code>Makie.pseudolog10</code>, <code>Makie.Symlog10</code>, <code>Makie.AsinhScale</code>, <code>Makie.SinhScale</code>, <code>Makie.LogScale</code>, <code>Makie.LuptonAsinhScale</code>, and <code>Makie.PowerScale</code>.</p><h4 id="highclip" tabindex="-1"><code>highclip</code> <a class="header-anchor" href="#highclip" aria-label="Permalink to &quot;`highclip` {#highclip}&quot;">​</a></h4><p><strong>Default:</strong> <code>automatic</code></p><p>The color for any value above the colorrange.</p><h4 id="lowclip" tabindex="-1"><code>lowclip</code> <a class="header-anchor" href="#lowclip" aria-label="Permalink to &quot;`lowclip` {#lowclip}&quot;">​</a></h4><p><strong>Default:</strong> <code>automatic</code></p><p>The color for any value below the colorrange.</p><h4 id="nan_color" tabindex="-1"><code>nan_color</code> <a class="header-anchor" href="#nan_color" aria-label="Permalink to &quot;`nan_color` {#nan_color}&quot;">​</a></h4><p><strong>Default:</strong> <code>:transparent</code></p><p>The color for NaN values.</p><h4 id="interpolate" tabindex="-1"><code>interpolate</code> <a class="header-anchor" href="#interpolate" aria-label="Permalink to &quot;`interpolate` {#interpolate}&quot;">​</a></h4><p><strong>Default:</strong> <code>true</code></p><p>Sets whether colors should be interpolated between pixels.</p><h4 id="orientation" tabindex="-1"><code>orientation</code> <a class="header-anchor" href="#orientation" aria-label="Permalink to &quot;`orientation` {#orientation}&quot;">​</a></h4><p><strong>Default:</strong> <code>(:down, :right)</code></p><p>A tuple <code>(d1, d2)</code> of directions describing how the first and second array dims run on the rectangle in data space. Each entry is one of <code>:up</code>, <code>:down</code>, <code>:left</code>, <code>:right</code>; exactly one must be vertical and one horizontal. Directions are stated relative to the image-conventional data space where y goes down and x to the right. <code>image</code> sets <code>yreversed = true</code> as an axis hint on freshly-created axes so that <code>:down</code> in <code>orientation</code> lines up with the visual downward direction on screen.</p><p>The default <code>(:down, :right)</code> matches the convention used by <code>FileIO.load</code>, NumPy, PIL, OpenCV and the like: <code>image[1, 1]</code> is the top-left pixel, the first array dim runs top-to-bottom, the second runs left-to-right. If your image was stored differently, for example because it was recorded by an unusual scientific instrument, adjust <code>orientation</code> accordingly (e.g. <code>(:left, :up)</code> for an image where <code>image[1, 1]</code> is the bottom-right pixel and the first dim runs along x).</p><h4 id="uv_transform" tabindex="-1"><code>uv_transform</code> <a class="header-anchor" href="#uv_transform" aria-label="Permalink to &quot;`uv_transform` {#uv_transform}&quot;">​</a></h4><p><strong>Default:</strong> <code>automatic</code></p><p>Sets a transform for uv coordinates, which controls how the image is mapped to its rectangular area. The attribute can be <code>I</code>, <code>scale::VecTypes{2}</code>, <code>(translation::VecTypes{2}, scale::VecTypes{2})</code>, any of <code>:rotr90</code>, <code>:rotl90</code>, <code>:rot180</code>, <code>:swap_xy</code>/<code>:transpose</code>, <code>:flip_x</code>, <code>:flip_y</code>, <code>:flip_xy</code>, or most generally a <code>Makie.Mat{2, 3, Float32}</code> or <code>Makie.Mat3f</code> as returned by <code>Makie.uv_transform()</code>. They can also be changed by passing a tuple <code>(op3, op2, op1)</code>.</p>',96)])])}const f=a(l,[["render",d]]);export{m as __pageData,f as default};
